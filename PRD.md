# PRD — 全屋定制订单自动拆 BOM 脚本

> 状态：v1.0（2026-09-07 由 call-matt 路由产出，依据 CLAUDE.md + rules.md 综合）
> 性质：**个人项目 / 模拟数据**（面试作品，非真实工厂系统）
> 一句话：订单 Excel 进 → 自动拆 BOM、算料、五金、成本 → 工艺拦截 → 入库 → API 供下游查询，完整演示"业务规则人工设计 + AI 写代码 + 人工验收"的岗位工作方式。

## 1. 背景与目标

**背景**：艾迪尔「AI 数字化应用专员」面试核心作品（JD 职责 3：拆单/系统对接）。真实拆单规则各厂不同，本项目用人工设计的简化规则模型演示方法与链路。

**目标**（按优先级）：
1. **G1 基础版**（必做）：50 单模拟订单 → 拆 BOM → 算料（含 1.08 损耗）→ 五金清单 → 工艺拦截 → SQLite 入库 + 采购清单 Excel
2. **G2 升级包**（必做，1 天）：SQLite → MySQL（SQLAlchemy + pymysql），新增 FastAPI 四接口查询服务，补全"数据一键流转"实证
3. **G3 演示资产**（必做）：README 量化对比（人工 X 分钟 vs 脚本 Y 秒，实测值）、3-5 分钟演示视频、2 条埋雷拦截截图

**非目标（Non-Goals）**：
- ❌ 真实对接酷家乐/三维家/ERP/MES（需企业认证）
- ❌ 板材开料排版优化（只按面积÷张数 ceil，不做套裁算法）
- ❌ 多柜型扩展（只做直型衣柜/悬浮电视柜/书柜三种）
- ❌ LangChain 等 JD 无关技术栈
- ❌ 抽屉盒体拆解（折进五金，简化口径，面试主动说明）

## 2. 用户与场景

| 用户 | 场景 |
|---|---|
| 拆单员（模拟） | 拿到订单 Excel，跑脚本得到采购清单，被拦截订单进 reject 复核 |
| 面试官（真实） | 看 README → 看演示视频 → 现场跑 main.py → 现场打开 /docs 调接口 |
| 下游系统（模拟） | 通过 FastAPI 查 BOM 明细/月度统计/拦截告警 |

## 3. 功能需求

### 3.1 数据输入（前置，Day 3）
- `orders.xlsx`：50 行，列 = order_id, customer, cabinet_type, width_mm, depth_mm, height_mm, board_material, door_count, drawer_count, shelf_count, hinge_spec
- **埋雷（硬性验收项）**：① 一单 `height_mm=2500`（侧板 550×2500，长 >2400）② 一单 `width_mm=2500` 2 门（门板 2500÷2=1250 >1200 宽）
- `prices.xlsx`：板材按张 + 五金按个/副/套/包的单价

### 3.2 核心流水线（Day 4-5，每模块 ≥3 条 pytest）

| # | 模块 | 需求 | 验收标准 |
|---|---|---|---|
| 1 | parser | 读 Excel，校验必填字段、尺寸>0 | 50 行全解析；空值/负数报错 |
| 2 | bom_engine | 按 rules.md §1 拆三种柜型 | 手算基准单（1800×550×2200，2门3抽4层）逐件核对一致 |
| 3 | hardware_calc | 按 rules.md §2 算五金 | 五金数量与规则一致（铰链 2×3=6 等） |
| 4 | optimizer | 按 rules.md §3 算料：主材/背板分开，×1.08，÷2.9768 向上取整 | 手算基准：主材 5 张、背板 2 张 |
| 5 | validator | 长≤2400、宽≤1200，超限整单拦截 | **2 条埋雷被拦截且原因话术正确**（演示爆点） |
| 6 | report | 采购清单 Excel + reject 清单 | 排版可读，成本合计与手算一致 |
| 7 | db | SQLite 入库 + 3 条统计 SQL | 板材 Top5 / 单均五金成本 / 拦截统计跑通 |
| 8 | main | CLI 串接全流程 | 端到端 50 单一次跑通 |

### 3.3 升级包（Day 6）

| # | 需求 | 验收标准 |
|---|---|---|
| 1 | docker-compose 起 MySQL 8.0（bom_db，root/bom123456:3306） | DBeaver 可连 |
| 2 | db.py 改造：连接串读 `DATABASE_URL`，建 bom_items / reject_orders 表 | 48 合规入库 + 2 埋雷进 reject；3 条统计 SQL 出数 |
| 3 | api.py 四接口：GET /orders（分页）、/orders/{id}/bom、/stats/monthly、/alerts/rejects | /docs 全调通；404 处理；埋雷订单能从 /alerts/rejects 查出 |
| 4 | API.md：每接口一段 + 开头 ASCII 架构图 | 面向"想接入数据流的同事"可读 |

**回退方案**：MySQL 卡超 2 小时 → 退 SQLite + FastAPI（链路完整即可）。

## 4. 非功能需求

- 每模块 pytest ≥3 条用例，小步验收后才进下一模块
- 量化数字只用实测（手算 5 单记耗时 vs 脚本 50 单记耗时）
- 全部产出物标注「个人项目 / 模拟数据」
- 面试口径："业务规则是简化模型，真实拆单规则以贵司工艺为准"

## 5. 关键依赖与前置条件

- **P0 阻塞项：rules.md 人工消化**（rules.md §6 的 7 条待确认清单答完才算 Day 3 验收通过；未消化前不开 Day 4 编码——这是"业务规则人工先行"纪律）
- Python 3.11+ / pandas / openpyxl / SQLAlchemy / pymysql / FastAPI / uvicorn
- Docker Desktop（升级包）
- 酷家乐画 1 柜截图（Day 3，输入源演示用）

## 6. 成功指标（冲刺视角）

| 指标 | 目标 |
|---|---|
| 端到端 | 50 单一次跑通，48 入库 + 2 拦截 |
| 拦截演示 | 埋雷原因话术正确，可截图 |
| 提效对比 | 实测手算 vs 脚本耗时写入 README |
| API | /docs 四接口全调通，含埋雷查询 |
| 交付 | Day 7 并入 `idir-ai-toolkit` 5 件套 |

## 7. 风险

| 风险 | 缓解 |
|---|---|
| rules.md AI 腔未消化，面试被打回 | Day 3 强制人工逐条确认 §6 清单 |
| 手册 4.93㎡ 系笔误（实为 2.9768） | 统一按 2.9768 执行，面试报数用此值 |
| Docker/MySQL 环境卡死 | 2 小时熔断退 SQLite |
| 进度落后 | 按看板升级主代理，砍项目 4 保项目 1 |
