# AGENTS.md — 人 / AI 分工约定

> 适用范围：项目根目录下所有任务。任何 agent 在本目录工作都必须遵守本文件的分工边界。
> 本项目是艾迪尔「AI 数字化应用专员」面试作品，核心叙事是「业务规则人工设计 + AI 写代码 + 人工验收调试」。

## 分工总原则

> 规则与验收是「人」的事，代码与执行是「AI」的事；凡是需要「讲出为什么」的地方，人都必须亲自下场。
- 业务规则由人设计并消化，agent 不猜业务。
- agent 的产出必须经人手算核对与验收通过，才算完成。
- 量化数字只用实测值，任何情况下不许编造。
- 一次只做一个模块，模块附 pytest 且测试全绿才算验收通过。

## 分工明细

| 环节 | 谁 | 关键动作 |
|---|---|---|
| 业务规则（rules.md） | 起草可交给 agent，但须经人消化改写 | 能讲出每条规则的「为什么」；未消化前不开编码 |
| 写代码 / 写测试 | agent | 按手册指令 + rules.md 对应章节实现，每个模块附 pytest |
| 造模拟数据 | agent 为主 | data/orders.xlsx（50 行含 2 埋雷）+ prices.xlsx |
| 环境与验证执行 | agent | venv、装依赖、跑 pytest、端到端跑 main.py |
| 手算核对 / 验收 | 人 | 对照 rules.md §5 手算基准逐项核对，确认后才进下一模块 |
| 量化实测 | 人 | 手算 5 单计时 vs 脚本 50 单计时，只报实测值 |
| 决策 / 取舍 | 人 | 埋雷设计、拦截粒度（整单 vs 单块）、切掉的功能、熔断降级 |
| 面试叙事 / 视频 / 截图 | 人 | README 量化对比、3-5 分钟视频、2 条拦截截图，讲成自己的作品 |

## 协作边界（agent 操作约束）

- **先给全上下文**：用户指令带上 rules.md 对应章节 + 明确数据结构，agent 不得自行发明业务规则。
- **小步验收**：一次只开发一个模块，验收通过后用户明确放行，才进入下一个，不批量铺开。
- **幻觉抽查**：agent 引用的库函数 / 参数需对照官方文档确认存在；数值结果抽样对照 rules.md §5 手算基准。
- **卡点熔断**：单模块卡超 2 小时 → 建议简化模型并说明合理性，不硬堆功能（对应任务书 Day 4 预警）。

## 项目红线（所有产出通用）

- 量化数字只用实测值，不外推、不编造。
- 项目一切产出标注「个人项目 / 模拟数据」。
- 面试口径固定为「业务规则是简化模型，真实拆单规则以贵司工艺为准」。
- 模块开发顺序与验收标准以 CLAUDE.md、Codex操作手册.md、IMPLEMENTATION_PLAN.md 为准。

## 常用命令

- 生成模拟数据：`.venv/bin/python scripts/make_sample_data.py`
- 跑测试：`.venv/bin/python -m pytest tests/ -q`
- 端到端：`.venv/bin/python main.py --input data/orders.xlsx --outdir output/`
